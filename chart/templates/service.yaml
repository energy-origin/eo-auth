apiVersion: v1
kind: Service
metadata:
  name: {{ include "ett-auth.fullname" . }}
  labels:
    {{- include "ett-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ett-auth.selectorLabels" . | nindent 4 }}
    component: {{ .Values.service.deployment }}
